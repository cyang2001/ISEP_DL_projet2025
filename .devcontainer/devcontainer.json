{
    "name": "Sign Language Recognition",
    "image": "sign_language_recognition",
    "runArgs": [
        "--gpus",
        "all"
    ],
    "workspaceFolder": "/workspace",
    "mounts": [
        "source=${localWorkspaceFolder},target=/workspace,type=bind,consistency=cached"
      ],
    "postCreateCommand": "pip install -r ./sign_language_recognition/requirements.txt",
    "remoteUser": "root"
}